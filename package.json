{
  "name": "image2svg",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.worker.json && tsc -p tsconfig.tests.json",
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.worker.json && tsc -p tsconfig.tests.json && vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "@fontsource-variable/manrope": "^5.3.0",
    "@phosphor-icons/core": "^2.1.1",
    "@types/node": "^26.5.0",
    "@types/pngjs": "^6.0.5",
    "pngjs": "^7.0.0",
    "svgo": "^4.1.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "esm-potrace-wasm": "^0.5.1",
    "vtracer-web": "^0.1.0"
  },
  "license": "GPL-2.0"
}
